Dear <%=approverName%>,

<% if (isMultipleSubServicesReq) { %>
A user at <%=orgName%> has requested changes to multiple sub-services.
<% } else { %>
A user at <%=orgName%> has requested changes to a sub-service.
<% } %> Please review and either approve or reject this request within the next 5 days.

Why am I receiving this email?

You are receiving this email, and others like it, because you are registered as an approver for <%=orgName%>.

<%=helpUrl%>

Request details

Name: <%=senderName%>
Email address: <%=senderEmail%>
Organisation: <%=orgName%>
Service: <%=serviceName%>
Sub-service changes: <% if (requestedSubServices.length > 0) { %>
                <% for (let x = 0; x < requestedSubServices.length; x++) { %>
                    <%= requestedSubServices[x] %> <br>
                <% } %>
            <% } else { %>
              No roles selected
            <% } %>


To approve this request:
1. Click the 'approve' button below to go to the request review page.
2. Either edit the user's selections or approve their request.
3. The end user will receive a 'sub-service added' email.
<%=approveUrl%>

To reject this request:
1. Click the 'reject' button below to go to the 'reject request' page.
2. Enter a reason for rejection.
3. Confirm the rejection and the end user will receive a 'request rejected' email.
<%=rejectUrl%>

Thank you,

DfE Sign-in