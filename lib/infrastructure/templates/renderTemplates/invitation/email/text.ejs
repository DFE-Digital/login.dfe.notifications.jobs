Dear <%=firstName%> <%=lastName%>

<% if (overrides && overrides.textBody) { %>
<%- overrides.textBody %>
<% } else { %>
<%if(selfInvoked){ %>
You've received this email because you've registered to join <%=serviceName%>. <%if (requiresDigipass) {%>To sign in, you'll need to have your token ready while your account is created.<%}%>
<% } else { %>
You have been invited to access DfE Sign-in <% if(approverEmail && orgName) { %> by <%=approverEmail%> at <%=orgName%>.<% } %>
<% } %>
<% } %>
<% if(isApprover) { %>
You will have an approver account. Approvers can manage other users at their organisation and add services to their account.
<% } %>

Please create your account by selecting the button below or by visiting
<%=returnUrl%>


Kind regards,

The Department for Education Sign-in Team

Need support? Contact us for further help <%=helpUrl%>

This is an automatically generated email; do not reply.
Received this email in error? No action is necessary.
