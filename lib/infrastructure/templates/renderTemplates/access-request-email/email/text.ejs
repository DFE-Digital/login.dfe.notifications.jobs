Dear <%=name%>

<% if (approved) { %>
    Your request to add <%=orgName%> to your DfE Sign-in profile has been approved.
<%} else { %>
    <% if (reason) { %>
    The rejection reason is: <%=reason%>
    <% } %>
     What should I do next?
    Submit an amended request, or request access to another organisation.
    If submitting an amended request, we recommend including information that will help identify you, such as your role.
<% } %>

Kind regards
DfE Sign-in
